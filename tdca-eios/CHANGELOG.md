# CHANGELOG — tdca-eios

## 2026-09-16 · 能力接口标识中性化

- 能力接口 `id` 字段值由内部编号改为中性能力英文名，与新增 `capability_id` 字段双轨同源（两者取值一致）。
- 字段名与路由不变，旧版本接口并存不下线；按字段名 / 路径对接的调用方零改动。
- 过渡期阈值 = 0（即时生效，SIMULATED 治理参数，预注册）；`capability_id` 与 `id` 双轨长期并存，不设到期移除。
- 逐处新旧对照由治理侧在册保管，不在公开仓列旧编号。
