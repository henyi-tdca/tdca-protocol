server {
    listen 80;
    server_name _;
    root /usr/share/nginx/web;
    index index.html;
    # SPA 路由回退：/auth /me /attestation/* 等深链全部回 index.html 由 React Router 接管
    location / {
        try_files $uri $uri/ /index.html;
    }
}
