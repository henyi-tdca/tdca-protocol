binding_id: SCENE-BIND-20260828-13B
schema: GSEQ-0601-律二 场景绑定 (立场运行时注入)
core_cop: STRATAGEM-CORE-20260828-13
scene:
  id: 行动安全
  entry: scenario/第01场景-场景思维协议.yaml
stance: 暴露警示
stance_note: 探测的暴露副作用须被抑制——观察即暴露，动作前先估泄露
inherit_cop: CHENGYU-69-20260828-69
behavior: 拟动作 → 估泄露 → 改信道 → 静默行
decision_override:
- if: 需获取信息且暴露本身即损失
  call: probe_exposure_coupling
negative_space:
- '⊗ 因惧暴露而完全放弃探测则陷入全盲'
mounts:
  scene: scenario/第01场景-场景思维协议.yaml
  knowledge_graph: chengyu/chengyu_manifest.yaml (第69条)
  skill: dispatch 挂点 probe_exposure_coupling
  contract: NCA 存证 + enforce_entry 准入门
  data_feed:  # 律三v2 动态状态通道 (绑定级细化)
    source: "自身暴露与信道状态流 (工业现场: 本侧操作日志/信道心跳)"
    variables:
    - name: 自身暴露度事件
      role: 泄露量化, 供'改信道/静默行'分支求值
    - name: 信道连通状态
      role: 静默可行性判定
    freshness_sla:
      max_staleness_s: 15
      on_breach: freeze
nsfl_version: V0.1
