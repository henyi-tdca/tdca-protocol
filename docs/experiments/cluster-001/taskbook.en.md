# TDCA Cluster-Orchestration Experiment — Task Book

> For any party able to orchestrate **local multi-session / multi-role** runs. **Re-runnable** —
> see the accompanying Reproduction Note. Experimental: **must not be cited as evidence that any
> institution is effective**; label everything **SIMULATED**.

## 1. Parameters
Groups: **A + B + C**; A = 3 rounds per orchestration; B = 1 round; C = 3 sub-agents.
Thresholds: **J-1 = 1.0; J-2 ≥ 0.9; J-3 ≤ 1; J-4 ≥ 0.8**. If a threshold is missed, report the
miss honestly — do not re-word the rubric, do not estimate.

## 2. Deliverables (4)
1. Per-group, per-round **artifact inventory** (count, paths, SIMULATED labels);
2. **J-1…J-4 values** with the numerator and denominator stated separately;
3. **Reproducible window** (start/end, local + UTC) and token accounting (monetary cost left blank);
4. Declaration: no external exposure, no push, no edits, **no shim-as-layer substitution**,
   **no padding**, **no estimates**.

## 3. Discipline
Retain all intermediate artifacts; never write "the institution is effective"; stop conditions as
in the Plan §6.
